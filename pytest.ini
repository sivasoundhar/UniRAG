[pytest]
# Puts the project root on sys.path so `from app...` imports resolve
# regardless of how pytest is invoked (bare `pytest`, `python -m pytest`,
# from an IDE, or in CI) — there's no app/__init__.py making this package
# installable, so this is what makes `tests/` able to import `app/` at all.
pythonpath = .
